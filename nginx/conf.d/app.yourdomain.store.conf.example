# Frontend Proxy Configuration (app.yourdomain.store)
#
# INSTRUCTIONS:
# 1. Copy this file: cp app.yourdomain.store.conf.example app.yourdomain.store.conf
# 2. Replace 'yourdomain.store' with your actual domain
# 3. After obtaining SSL certificates, uncomment the SSL server block
# 4. Reload nginx: docker compose exec nginx nginx -s reload

# HTTP - Redirect to HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name app.yourdomain.store;

    # Allow Let's Encrypt ACME challenges
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect all other HTTP traffic to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS - Main frontend configuration
# UNCOMMENT THIS BLOCK AFTER OBTAINING SSL CERTIFICATES
#server {
#    listen 443 ssl;
#    listen [::]:443 ssl;
#    http2 on;
#    server_name app.yourdomain.store;
#
#    # SSL certificates (managed by certbot)
#    ssl_certificate /etc/letsencrypt/live/app.yourdomain.store/fullchain.pem;
#    ssl_certificate_key /etc/letsencrypt/live/app.yourdomain.store/privkey.pem;
#
#    # SSL configuration
#    include /etc/nginx/snippets/ssl-params.conf;
#
#    # Security headers
#    include /etc/nginx/snippets/security-headers.conf;
#
#    # HSTS (enable after confirming SSL works)
#    # add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#    # Rate limiting for frontend
#    limit_req zone=frontend_limit burst=40 nodelay;
#    limit_conn addr 20;
#
#    # Logging
#    access_log /var/log/nginx/app.yourdomain.store.access.log;
#    error_log /var/log/nginx/app.yourdomain.store.error.log;
#
#    # Proxy to frontend container
#    location / {
#        proxy_pass http://pantinventory_frontend:80;
#        include /etc/nginx/snippets/proxy-params.conf;
#
#        # Cache static assets
#        location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
#            proxy_pass http://pantinventory_frontend:80;
#            include /etc/nginx/snippets/proxy-params.conf;
#            expires 1y;
#            add_header Cache-Control "public, immutable";
#        }
#    }
#
#    # Health check endpoint
#    location /health {
#        access_log off;
#        return 200 "healthy\n";
#        add_header Content-Type text/plain;
#    }
#}
